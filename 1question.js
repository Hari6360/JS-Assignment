// 1.  Create a variable.js file and declare variables and assign string, boolean, undefined and null data types, Display all the value with their data type.

// solution:

// string

let str = "Apple";

console.log(
	`data typeof of the value is ${typeof str}and the value is: ${str}`
);

// boolean value

let boolean = true;

console.log(
	`data typeof of the value : ${typeof boolean}and the value is: ${boolean}`
);

let wrong = false;

console.log(
	`data typeof of the value is ${typeof wrong}and the value is: ${wrong}`
);

//undefined

let defined;

console.log(
	`data typeof of the value is ${typeof defined}and the value is: ${defined}`
);
// null

let zero = null;

console.log(
	`data typeof of the value is ${typeof zero}and the value is: ${zero}`
);
